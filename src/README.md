# Source directory

- `dashboard.py` — Streamlit entry point. Its location is intentionally unchanged to preserve the deployed app configuration.
- `pipeline/` — active numbered collection and transformation notebooks.
- `archive/` — legacy collection experiments that are not part of the supported workflow.
